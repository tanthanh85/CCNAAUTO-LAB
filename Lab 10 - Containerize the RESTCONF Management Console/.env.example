FLASK_SECRET_KEY=replace-with-a-random-value
INVENTORY_ENCRYPTION_KEY=replace-with-a-fernet-key
# Leave empty when the router uses a publicly trusted certificate.
# For a private CA, copy its PEM file to certificates/router-ca.pem and use:
# LAB_CA_BUNDLE=/certificates/router-ca.pem
LAB_CA_BUNDLE=
